# Upstream relationship

This repository is a fork of [`dotnet/extensions`](https://github.com/dotnet/extensions) that publishes the WantsACracker migration packages. It is deliberately narrow: the only intended divergence from upstream is the mechanical substitution of Polly for the independently implemented `WantsACracker` APIs in the HTTP resilience integration, plus the accompanying package, namespace, and metadata renames.

## Branches

- `main` — upstream-tracking baseline. Branch-protected (no force pushes, deletions, or fork syncing). Never commit product changes here.
- `wants-a-cracker` — long-lived branch where all product work happens. Submodule pins in downstream repositories point at this branch.

## Baseline

- Upstream: `dotnet/extensions`
- Baseline commit: `cc597aa24bf108c38a6a59d09555765575d11cb3` — *Validate path segments in Azure storage result store and response cache (#7718)*
- Pinned on: 2026-08-26
- Polly versions at baseline (from `eng/Versions.props`): `9.0.18` (default), `10.0.10` (net10.0), `8.0.29` (net8.0) — superseded / corrected: these three values are the `MicrosoftExtensionsHttpPolly*Version` properties in `eng/Versions.props`, i.e. the versions of the `Microsoft.Extensions.Http.Polly` wrapper package, which neither `Microsoft.Extensions.Resilience` nor `Microsoft.Extensions.Http.Resilience` references; per the 2026-08-28 Polly package-graph finding, every Polly package in both projects actually resolves to `8.4.2` on all TFMs (pinned TFM-independently in `eng/packages/General.props`)

## Relevant paths

The only paths expected to diverge from upstream:

| Path | Reason for divergence |
|---|---|
| `src/Libraries/Microsoft.Extensions.Resilience/` | Rename to `WantsACracker.Extensions.Resilience`; replace Polly references with WantsACracker equivalents |
| `src/Libraries/Microsoft.Extensions.Http.Resilience/` | Rename to `WantsACracker.Extensions.Http.Resilience`; replace Polly references with WantsACracker equivalents |
| `test/Libraries/Microsoft.Extensions.Resilience.Tests/` | Follows the source rename; behaviour preserved |
| `test/Libraries/Microsoft.Extensions.Http.Resilience.Tests/` | Follows the source rename; behaviour preserved |
| `bench/Libraries/Microsoft.Extensions.Resilience.PerformanceTests/` | Follows the source rename |
| `bench/Libraries/Microsoft.Extensions.Http.Resilience.PerformanceTests/` | Follows the source rename |
| `eng/Versions.props` | Polly version entries replaced by WantsACracker package versions; `Microsoft.Extensions.ObjectPool` central pins bumped one patch (divergence 8) |
| `eng/packages/General.props` | Central package versions for the three un-renamed sibling dependencies of the renamed packages pinned to the published 10.9.0 line (divergence 8) |
| `samples/` | Additive runnable sample for the renamed Http.Resilience package (divergence 4); upstream has no `samples/` directory |
| `smoke/` | Additive checked-in clean-consumer smoke script + release-guard script + consumer app source (divergences 9 and 10); upstream has no `smoke/` directory |
| `.github/workflows/wantsacracker-release-guard.yml` | Additive WantsACracker-specific workflow wrapping the release-guard script (divergence 10); the upstream workflows are untouched |

Everything else — build infrastructure, other libraries, analyzers, documentation — must remain byte-identical to upstream. If a change outside this list becomes necessary, it must be justified in the commit message and logged in the divergence list below.

## Divergence rules

- Changes must be mechanical: package IDs, namespaces, and dependency references.
- Preserve upstream behaviour, structure, tests, metadata, and style unless a rename requires the change.
- No refactors, feature work, or unrelated cleanup in this branch.
- Every divergence carries a short rationale in its commit message.

## Divergences from baseline

1. **2026-08-29 — Rename resilience package IDs and public namespaces to `WantsACracker.Extensions.*`** (first migration slice; renames only, Polly untouched):
   - `src/Libraries/Microsoft.Extensions.Resilience/` and `src/Libraries/Microsoft.Extensions.Http.Resilience/`: the project file, the API-surface `.json` and (Http.Resilience) the `buildTransitive/*.targets` file are renamed to `WantsACracker.Extensions[.Http].Resilience.*`, which moves `AssemblyName`, `PackageId` and `RootNamespace` to the `WantsACracker` identity (the API-staging analyzer and the `$(MSBuildProjectName).json` / `buildTransitive\$(MSBuildProjectName).targets` build machinery key off the project file name, so the files had to follow). All public namespaces move from `Microsoft.Extensions[.Http].Resilience` to `WantsACracker.Extensions[.Http].Resilience`; the extension classes living in `Microsoft.Extensions.DependencyInjection` and `System.Net.Http` keep their namespaces. `EnablePackageValidation` is set to `false` in both source projects because no published baseline version exists under the new package IDs yet.
   - `test/Libraries/Microsoft.Extensions.Resilience.Tests/` and `test/Libraries/Microsoft.Extensions.Http.Resilience.Tests/`: projects, test namespaces, the proto `csharp_namespace` and the embedded path to the renamed `.targets` file follow the source rename; behaviour and assertions preserved.
   - `bench/Libraries/Microsoft.Extensions.Resilience.PerformanceTests/` and `bench/Libraries/Microsoft.Extensions.Http.Resilience.PerformanceTests/`: follow the source rename, as declared in the relevant-paths table above.
   - `src/ProjectTemplates/Microsoft.Extensions.AI.Templates/Microsoft.Extensions.AI.Templates.csproj` and `test/Libraries/Microsoft.Extensions.AotCompatibility.TestApp/Microsoft.Extensions.AotCompatibility.TestApp.csproj`: mechanical project-reference path updates only.
   - Rationale: required so the published packages carry the `WantsACracker` identity before the Polly → WantsACracker dependency swap (next slice).
2. **2026-08-30 — Swap the resilience libraries from Polly to the WantsACracker core package** (commit `3713f73be`; the mechanical `Polly` → `WantsACracker` using/type/namespace substitutions dominate the change set — the points below are the deliberate, non-mechanical divergences, each required for the substitution, with upstream behaviour, structure, and tests otherwise preserved):
    - `src/Libraries/Microsoft.Extensions.Http.Resilience/Internal/HttpKey.cs`: `HttpKey` is now `public` (upstream: `internal`). The delay-signed fork assembly cannot grant an effective `InternalsVisibleTo` to the *unsigned* `Castle DynamicProxyGenAssembly2` that Moq emits for `ResiliencePipelineProvider<HttpKey>` (the unsigned `WantsACracker` base type makes Castle emit an unsigned proxy, which can never match the keyed IVT), so the Moq-based pipeline-selection tests require the key type to be visible. The type is recorded in the `WantsACracker.Extensions.Http.Resilience.json` API baseline.
    - `src/Libraries/Microsoft.Extensions.Http.Resilience/Resilience/ResilienceHttpClientBuilderExtensions.Resilience.cs` (`CreatePipelineSelector`, null-key branch): builds the pipeline **eagerly** at handler creation (options validation fails fast with `OptionsValidationException`, as the reference implementation does) and then **fetches the pipeline per request** instead of caching the instance — the `WantsACracker` registry replaces the pipeline instance on dynamic reloads, so a cached instance would go stale (the reference implementation keeps one instance alive by swapping its internal component).
    - `HttpStandardResilienceOptionsCustomValidator` (`Resilience/Internal/Validators/`) and `HttpStandardHedgingResilienceOptionsCustomValidator` (`Hedging/Internals/Validators/`): the hand-written `IValidateOptions` validators now also mirror the range checks the `WantsACracker` `Add*` builder methods perform. The `WantsACracker` strategy options carry no data annotations, so the generated `[OptionsValidator]` code is empty; without these checks an invalid configuration would surface as an `ArgumentOutOfRangeException` at pipeline-build time instead of an `OptionsValidationException` at options-resolution time. The hedging delay is intentionally **not** range-checked: a negative delay (canonically `Timeout.InfiniteTimeSpan`) is a supported "wait indefinitely" configuration, matching the reference implementation.
    - `WantsACracker.Extensions.Resilience.csproj` / `WantsACracker.Extensions.Http.Resilience.csproj`: `CS8002` is suppressed in the two renamed src csprojs (and the two test csprojs) — the delay-signed assemblies reference the intentionally unsigned `WantsACracker` package, and .NET does not enforce strong-name verification. `DisableStrongNamer=true` is set in the two test csprojs so StrongNamer does not re-sign the unsigned reference and change its identity.
    - `NuGet.config` + `.gitignore` + `eng/local-packages/WantsACracker.0.1.0.nupkg`: a committed local package feed — the packed core package is committed, `NuGet.config` maps `WantsACracker` to the `eng/local-packages` source (via the `wants-a-cracker-local` `packageSource` entry and a matching `packageSourceMapping` pattern), and `.gitignore` re-includes `!eng/local-packages/*.nupkg` under its `*.nupkg` rule (the package version is pinned in `eng/packages/General.props`). **Local builds require the committed `NuGet.config` local feed** — a stale `wantsacracker/0.1.0` copy in the global NuGet cache can shadow the committed nupkg.
    - `WantsACracker.Extensions.Http.Resilience.csproj`: `SYSLIB1202` is suppressed — the `Http*StrategyOptions` types are empty subclasses of the `WantsACracker` strategy-options types, which validate their values in the `Add*` builder methods rather than via data annotations, so the data-validation source generator finds no validatable members on the subclasses and the warning is suppressed.
3. **2026-08-30 — Set WantsACracker package metadata on the two renamed src projects** (project-level changes in the two renamed csprojs only; no shared props/targets touched):
    - `WantsACracker.Extensions.Resilience.csproj` / `WantsACracker.Extensions.Http.Resilience.csproj`: `Description` updated to WantsACracker-appropriate copy (the Resilience one still described "the Polly libraries" — stale after the swap); `Authors=WantsACracker contributors` (overriding the Arcade default `Microsoft`) and `RepositoryUrl=https://github.com/alefranz/Extensions` (this fork, where the source lives) set explicitly, and `PackageProjectUrl=https://github.com/alefranz/WantsACracker-Development` overriding the shared `https://dot.net/` from `eng/MSBuild/Packaging.props`; `PackageIcon` cleared to suppress the Arcade-default .NET Foundation icon that otherwise gets embedded in every package (WantsACracker packages carry no icon yet — the branding guide is wordmark-first and avoids .NET/Microsoft visual resemblance); `CopyrightMicrosoft` overridden to "© WantsACracker contributors" — Arcade's `ProjectDefaults.targets` (imported after the project body) unconditionally sets `Copyright=$(CopyrightMicrosoft)`, so without this override the packed nuspec carries "© Microsoft Corporation. All rights reserved." (Arcade's default, overriding even `eng/MSBuild/Packaging.props`'s `$(CopyrightNetFoundation)`) — a Microsoft identity claim the product's "not affiliated with Microsoft" footer rules out.
    - New `THIRD-PARTY-NOTICES.TXT` in each project directory, packed at the package root via a `None` item (`Pack="true" PackagePath="\"`): attributes the dotnet/extensions-derived code to the .NET Foundation (MIT), lists the MIT-licensed direct dependencies (including the WantsACracker core package and the same-repo derived libraries), and carries the "independently implemented, not affiliated" footer. The shared repo-root `THIRD-PARTY-NOTICES.TXT` is not packed by the Arcade SDK, so per-package files are the narrowest mechanism.
    - Intentionally **not** changed: `PackageLicenseExpression=MIT` (already correct in shared `eng/MSBuild/Packaging.props`, matching the upstream MIT notice the fork preserves).
    - Rationale: the two published packages must carry the WantsACracker identity (repository URLs, authors, descriptions, copyright, notices) without claiming Microsoft/upstream identity, while preserving the upstream MIT licensing/attribution.
4. **2026-08-30 — Add a runnable sample for the renamed Http.Resilience package** (additive-only: a new `samples/` directory; no existing upstream file is changed):
    - New `samples/Libraries/WantsACracker.Extensions.Http.Resilience.Sample/` (`WantsACracker.Extensions.Http.Resilience.Sample.csproj` + `Program.cs`) and `samples/Directory.Build.props`. The sample wires the canonical usage pattern — `AddHttpClient("sample")` + `AddStandardResilienceHandler()` on a named client — and exercises it against an in-process `HttpListener` (the first request fails with a transient 503 that the standard handler's retry strategy recovers from; a second request succeeds on the first attempt), so it runs fully offline with no network or external server.
    - `samples/Directory.Build.props` follows the fork's executable convention from `bench/Directory.Build.props` (`OutputType=Exe`, single `$(LatestTargetFramework)` TFM, `GenerateProgramFile=false`). The sample references the source project directly (`ProjectReference` to `WantsACracker.Extensions.Http.Resilience.csproj`, the same convention as the test and bench projects — the committed local nupkg feed under `eng/local-packages` carries only the core `WantsACracker` package, so it cannot stand in for the migration package).
    - `samples/Directory.Build.props` suppresses `RT0000` (samples generate no documentation files — the same suppression `test/Directory.Build.props` applies) and `SA1636` (the file-header copyright rule, which expects the upstream .NET Foundation header; sample files are fork-specific additions and carry none).
    - Rationale: the sample must consume `WantsACracker.Extensions.Http.Resilience`, the package this branch owns and publishes; the fork already wires the local `WantsACracker` 0.1.0 nupkg feed (`eng/local-packages` + `NuGet.config`); and the Core repository must stay free of fork knowledge (portability), which rules out the Core repo or cross-repository working documents as the sample's home. Being purely additive, it changes no upstream content and keeps the divergence narrow.
5. **2026-09-03 — The standard handler no longer retries non-replayable request bodies** (commit `b14cc8496`; non-mechanical — a safety-gate divergence, not a rename):
    - `src/Libraries/Microsoft.Extensions.Http.Resilience/Resilience/ResilienceHttpClientBuilderExtensions.StandardResilience.cs` (the `AddStandardResilienceHandler()` path): upstream composes the retry strategy unconditionally. The fork composes two standard pipelines — the ordinary one with the retry strategy and an identical one without it — and selects the pipeline **per request** via `HasReplayableContent`: content is replayable only when it is `null` or one of the built-in bufferable `HttpContent` types (`StringContent`, `ByteArrayContent`, `FormUrlEncodedContent`, `JsonContent`, `MultipartContent`); every other content shape (non-seekable `StreamContent`, custom `HttpContent` subclasses) is sent through the no-retry pipeline and is sent at most once.
    - The fork's `HasReplayableContent` predicate is a **deliberate copy** of the replayability rule in the `WantsACracker` core package's `StandardResilienceHandler` (the Core method is `internal`, so the rule cannot be shared as code): **if the Core rule changes, this copy must be updated in lockstep** (keep-in-sync obligation).
    - `WantsACracker.Extensions.Http.Resilience.csproj`: a `System.Net.Http.Json` reference scoped to `netstandard2.0`/`net462` only, where `JsonContent` is not in the BCL (mirroring the WantsACracker core package; the version resolves from the existing central `eng/Versions.props`, no new pin).
    - Rationale: safety gate — the standard handler must never silently re-send a request body that cannot be replayed, and the replayability rule must match the behaviour users already get from the core's standalone standard handler.
6. **2026-09-03 — Document the replayability policy in the package README** (docs-only; the `src/Libraries/Microsoft.Extensions.Http.Resilience/README.md` gains a "Retrying requests with content" section stating the replayable/non-replayable rule exactly as implemented in divergence 5 — absent content and the built-in bufferable `HttpContent` types are replayable; everything else, including non-seekable `StreamContent` and custom `HttpContent` subclasses, is non-replayable and sent at most once. Resolves the "(see UPSTREAM.md)" pointer in the `System.Net.Http.Json` `ItemGroup` comment added in `b14cc8496`.)
7. **2026-09-04 — Align all package versions to the preview identity `0.1.0-preview.1`** (mechanical version alignment for the first preview pack):
    - `eng/packages/General.props`: the `WantsACracker` central package version pin moves `0.1.0` → `0.1.0-preview.1`, and the committed local feed package is re-packed under the new name `eng/local-packages/WantsACracker.0.1.0-preview.1.nupkg` (the `0.1.0` nupkg is removed; `NuGet.config`'s feed entry and `packageSourceMapping` are name-independent and unchanged).
    - Root `Directory.Build.targets`: a final `PropertyGroup`, conditioned on `'$(Authors)' == 'WantsACracker contributors'` (set by exactly the two renamed src csprojs, per divergence 3), sets `Version=0.1.0-preview.1`. This must run **after** Arcade's `Version.BeforeCommonTargets.targets` (imported at the top of this file), which unconditionally recomputes `Version` from `eng/Versions.props` (`10.10.0-<label>`, label `dev` for local builds) — a `Version` in either csproj body is clobbered, and no Arcade input property can produce a plain `preview.1` label in a local build. All other projects are unaffected (verified to still evaluate to `10.10.0-dev`).
    - Rationale: the three preview packages (core + the two migration packages) must pack at one preview version, `0.1.0-preview.1`, matching the install snippets in the package READMEs; no upstream file outside this list is touched.
8. **2026-09-04 — Consume the un-renamed sibling dependencies from the published 10.9.0 line** (mechanical dependency re-pin for the first preview pack):
    - `WantsACracker.Extensions.Resilience.csproj`: the in-tree `ProjectReference`s to `Microsoft.Extensions.Diagnostics.ExceptionSummarization` and `Microsoft.Extensions.Telemetry.Abstractions` are replaced by `PackageReference`s to the published `10.9.0` packages.
    - `WantsACracker.Extensions.Http.Resilience.csproj`: the in-tree `ProjectReference` to `Microsoft.Extensions.Http.Diagnostics` is replaced by a `PackageReference` to the published `10.9.0` package (the `ProjectReference` to the renamed `WantsACracker.Extensions.Resilience` project is unchanged).
    - `eng/packages/General.props`: central `PackageVersion` pins `10.9.0` for `Microsoft.Extensions.Diagnostics.ExceptionSummarization`, `Microsoft.Extensions.Http.Diagnostics`, and `Microsoft.Extensions.Telemetry.Abstractions`.
    - `eng/Versions.props`: the three `Microsoft.Extensions.ObjectPool` central pins are bumped one patch (`9.0.18` → `9.0.19`, `10.0.10` → `10.0.11`, `8.0.29` → `8.0.30`) — the minimal versions the published `Microsoft.Extensions.Telemetry 10.9.0` (a transitive dependency of the re-pinned siblings) requires; without the bump the `Shared.csproj` ObjectPool reference triggers a `NU1605` downgrade error in the renamed projects.
    - `WantsACracker.Extensions.Resilience.csproj` / `WantsACracker.Extensions.Http.Resilience.csproj`: `SuppressTfmSupportBuildWarnings=true` — the published sibling packages ship `net462`/`netstandard2.0` assets; this only silences the repo-standard "not tested with net462" compat warning the package `buildTransitive` targets emit (same suppression `Microsoft.Extensions.Caching.Hybrid` already carries in-repo).
    - Rationale: the local-train version `10.10.0-dev` of the un-renamed sibling libraries is published nowhere, so the packed `0.1.0-preview.1` packages declared dependencies no clean consumer could restore (`NU1102` on the three siblings). The siblings are un-renamed, un-modified, and outside the three-package preview scope, so pinning the renamed packages to the nearest published line (`10.9.0`) keeps the preview graph resolvable from public feeds without widening the divergence. The in-tree sibling projects and their test/bench projects are untouched (tests keep resolving the in-tree projects, whose `10.10.0-dev` version wins over the `10.9.0` package).

9. **2026-09-05 — Add the checked-in clean-consumer smoke test** (additive-only: a new `smoke/` directory; no existing upstream file is changed):
   - New `smoke/run-clean-consumer.sh`: one command that (a) builds and packs the two renamed src projects at `0.1.0-preview.1` via the repo build flow, (b) collects exactly the three preview nupkgs (the committed `eng/local-packages` core nupkg, sha256-verified, + the two fresh packs) into a temporary local feed, (c) restores/builds the checked-in consumer app against that feed + nuget.org from an empty isolated `NUGET_PACKAGES` cache, (d) asserts the exact resolved package set/versions and that no Polly package resolved or downloaded, and (e) runs the offline scenario — the first request to the named `HttpClient` gets `503`, the standard handler's retry succeeds, the app exits `0` — exiting non-zero on any failure at any stage.
   - New `smoke/clean-consumer/` (`consumer.csproj` + `Program.cs`): a minimal `net8.0` consumer using **package references** (never project references) to exactly the three `0.1.0-preview.1` packages; the named client is wired via `AddHttpClient("smoke") + AddStandardResilienceHandler()` against an in-process loopback `HttpListener` (the same offline 503 → retry scenario as the `samples/` runnable sample from divergence 4, but consuming the packed packages instead of the source projects). The script copies the sources to a temporary workspace and builds them out-of-tree, so no build output ever lands in the repository (pack output goes to the gitignored `artifacts/` tree); the feed-pinning `nuget.config` is generated by the script in that workspace.
   - New `smoke/README.md`: documents the command and its intended step-4 CI entry point (invoke the same script unchanged as a standalone job after the build/test steps).
   - Rationale: the accepted 2026-09-04 three-feed scratch probe must become a durable, CI-callable package-level release guard before the step-4 CI job is added. The fork is the convergence point where all three preview packages exist (the committed core feed nupkg + the two packable projects), so it is the natural home; the Core repository must stay free of fork knowledge (portability — same rationale as divergence 4). Being purely additive, it changes no upstream content and keeps the divergence narrow.

10. **2026-09-05 — Add the minimum release guard (preview-path step 4)** (additive-only: a new script under the existing `smoke/` directory and one WantsACracker-named workflow; no existing file — upstream or fork — is changed):
    - New `smoke/run-release-guard.sh`: one checked-in command that, in order, (a) builds the two renamed src projects (Release, all TFMs, repo build flow), (b) runs the two renamed test suites on `net8.0`, (c) checks the API surface by asserting the committed API-baseline jsons are untouched after the analyzer-enabled build, (d) invokes the unchanged clean-consumer smoke (`bash smoke/run-clean-consumer.sh`, divergence 9) — pack at `0.1.0-preview.1`, exactly-three-nupkg feed, empty isolated cache, exact resolved graph with zero Polly, the offline 503 → retry → 200 scenario — and (e) runs package-content checks on the three preview nupkgs (nuspec id/version, MIT expression licence, README + THIRD-PARTY-NOTICES at the package root, `WantsACracker 0.1.0-preview.1` declared in every TFM dependency group, no Polly anywhere). It exits non-zero on any failure at any stage and leaves no tracked build output.
    - New `.github/workflows/wantsacracker-release-guard.yml`: the thin CI wrapper — on pushes to `wants-a-cracker` and PRs targeting it, it checks out the branch and runs `bash smoke/run-release-guard.sh`. The upstream `dotnet/extensions` workflows are untouched; no publishing, signing, matrix, or other platform expansion.
    - `smoke/README.md`: documents the guard as the step-4 entry point and its relationship to the smoke script.
    - Rationale: preview-path step 4 requires a minimum automated guard for build/test/pack, API-surface comparison, package-content checks, and the smoke test, while avoiding a general CI expansion. The fork is the convergence point where all three preview packages exist (divergence 9 rationale), so it is the natural home; a single script + a thin workflow keeps the guard locally runnable with a plain checkout (the same property as the smoke command). Being purely additive, it changes no upstream content and keeps the divergence narrow.

11. **2026-09-05 — Add the differential compatibility harness (preview-path step 5, part A)** (additive-only: new files under the existing `smoke/` directory; no existing file — upstream or fork — is changed):
    - New `smoke/run-differential.sh`: one command that, in order, (a) builds and packs the two renamed src projects at `0.1.0-preview.1`, (b) collects exactly the three preview nupkgs (the committed `eng/local-packages` core nupkg, sha256-verified, + the two fresh packs) into a temporary feed, (c) restores/builds/runs the scenario app **WantsACracker-side** from an empty isolated `NUGET_PACKAGES` and asserts the resolved graph contains no Polly, (d) transforms the single scenario source into the **reference side** (Polly 8.4.2) via the marker-pair swap — verified to be exactly the pair swap: paired marker counts, no leftover markers, and a plain-diff line count of 3 × pair count — restores/builds/runs it against `Microsoft.Extensions.Http.Resilience 10.9.0` from nuget.org, and asserts the resolved graph carries **exactly** `Polly.Core`/`Polly.Extensions`/`Polly.RateLimiting` `8.4.2`, and (e) compares each per-scenario outcome against the expected one for its side, printing MATCH / documented-DIFFERENCE per scenario and failing (non-zero exit) on anything else. All intermediate state is temporary; the apps are built out-of-tree, so no tracked build output is left behind.
    - New `smoke/differential/` (`Program.cs`, `wantsacracker.csproj`, `polly.csproj`): five fully offline P0 loopback scenarios (retry recovers a transient 503; attempt timeout fires and the retry recovers; the total-request timeout fails; the circuit breaker opens after two failed executions; a non-replayable `StreamContent` body and the retry gate) through the shared seam — a named `HttpClient` wired with `AddStandardResilienceHandler()`. `Program.cs` encodes both sides in one file via paired marker blocks (`// @@WAC@@` active line + `//@@POLLY@@` comment counterpart); today exactly one line differs (the options-type alias — `WantsACracker.Extensions.Http.Resilience.HttpStandardResilienceOptions` vs `Microsoft.Extensions.Http.Resilience.HttpStandardResilienceOptions`, same type name). The csprojs use package references only (never project references) and are built out-of-tree by the script.
    - Reference-side version choice, recorded as fact: no `8.4.x` version of `Microsoft.Extensions.Http.Resilience` exists on nuget.org (8.4.0/8.5.0 resolve Polly **8.3.0**), so the 8.4.2 compatibility surface is driven through `10.9.0` — the published sibling of the fork's 10.10.0-dev upstream baseline (same line the un-renamed sibling deps are pinned to, divergence 8), which pins exactly Polly 8.4.2; the script's exact-8.4.2 graph assertion keeps it honest.
    - `smoke/README.md` (fork-owned, divergence 9): documents the harness, the per-scenario results (four MATCH, one documented difference — the no-retry gate on non-replayable bodies, divergence 5), and the surface facts proven at the 8.4.2 reference (identical option names, including the Polly.Core 8.4.2 breaker names `FailureRatio`/`MinimumThroughput`/`SamplingDuration`/`BreakDuration` and the `BrokenCircuitException` rejection; the `Timeout.AttemptTimeoutDuration`/`FailureThreshold`-style names belong to the older 8.4.0/8.5.0 (Polly 8.3.0) and pre-8.3.0 surfaces and were a superseded assumption).
    - Rationale: preview-path step 5 requires differential evidence for the supported P0 standard-handler surface; the fork is the convergence point where all three preview packages exist (divergence 9 rationale) and the only place a dual-side harness can restore both package sets. Being purely additive, it changes no upstream content and keeps the divergence narrow.

## Rebase and sync policy

- **Quarterly:** compare the relevant paths to upstream `main`; log each change as adopted, deferred, or rejected in the divergence list above.
- **Before every .NET major release:** rebase `wants-a-cracker`, run conformance tests, and decide whether to publish a matching release.
- Rebases are done against the protected `main` baseline, which is fast-forwarded from upstream rather than rebased itself.
